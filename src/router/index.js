import Index from '../components/Index'

export default {
    mode: 'history',
    routes: [
        {
            name: 'Index',
            path: '/',
            component: Index
        },
    ]
}
